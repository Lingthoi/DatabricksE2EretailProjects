# DatabricksETEretailProjects
Azure Databricks  ETE retail data engineering pipeline using Medallion Architecture (Bronze → Silver → Gold). Uses Auto Loader for incremental ingestion and Delta Lake for storage, with Unity Catalog for governance. Models SCD Type 1 &amp; 2 dimensions and a Fact Orders table into an analytics-ready star schema.
